I used mkdir to create the directory structure. Before modification, the directory had standard default permissions (shown by the first ls -ld). I used the chmod 700 command to restrict permissions so that only the owner has read, write, and execute access, which securely protects the project data from other users. The second ls -ld confirmed this modification was successful. Finally, running umask displayed the default permission mask used by my system.